<div class="row" id="newsStories">
    {{#each newsStuff}}
        <div class="col-md-8" id="rowArea">

            <h5>{{title}}</h5>
            <button type="submit" id="addNote" data-id="{{_id}}" title-id="{{title}}" class="btn btn-info btn-sm saveOrDelete"> Add Note </button>
            <button type="submit" id="deleteFromSaved" data-id="{{_id}}" title-id="{{title}}" class="btn btn-danger btn-sm saveOrDelete"> Delete </button>
            <a class="linktags" href= {{link}}> {{link}} </a>
            {{!--<br>
            {{pic}}--}}
        </div>
    {{/each}}
    
    <div class="col-md-4">
        <form id="noteForm">
            <div class="form-group" id="noteSection">
                <label for="exampleTextarea" id="noteArea">Add A Note To The Article?</label>
                <h5>
                    <span id="title"></span>
                </h5>
                <textarea class="form-control" id="noteTextArea" rows="5"></textarea>
                <br>
                <button type="submit" id="saveNote" data-id="" title-id="" class="btn btn-success saveThisNote"> Save Note </button>
                <button type="submit" id="deleteNote" data-id="" title-id="" class="btn btn-danger deleteThisNote"> Delete Note </button>
            </div>
        </form>
    </div>
</div>